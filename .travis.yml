os:
    - linux # Supports Ubuntu 16.04 by default
env:
    - PYTHONPATH=`pwd`
language: python 
python: # 3.0 is not supported on Travis CI
    - "3.6"
    - "3.7"
install:
    - python setup.py install
script: pytest tests/
